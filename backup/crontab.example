# Daily at 02:30, run AzCopy to sync backup folder to Storage (replace placeholders)
30 2 * * * /usr/bin/azcopy sync "/var/lib/unifi/backup" "https://<storage-account>.blob.core.windows.net/<container>/<path>" --recursive
